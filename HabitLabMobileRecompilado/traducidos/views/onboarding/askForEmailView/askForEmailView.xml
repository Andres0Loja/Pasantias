<Page loaded="pageLoaded" unloaded="pageUnloaded" navigatingTo="navigatingTo" actionBarHidden="true">

<StackLayout id="first">

	<Label text="{{ L('views.onboarding.askForEmailView.title') }}" class='title' textWrap='true'></Label>
	<Label text="{{ L('views.onboarding.askForEmailView.text.part1') }}" textWrap="true"></Label>
	<Label text="{{ L('views.onboarding.askForEmailView.text.part2') }}" textWrap="true"></Label>
	<Button text="{{ L('views.onboarding.askForEmailView.button') }}" onTap="requestIdToken"></Button>
</StackLayout>

</Page>
